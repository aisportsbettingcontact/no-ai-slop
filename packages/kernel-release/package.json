{
  "name": "@nas/kernel-release",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TC-10: Release-Gate Correctness. Consumes only authenticated evidence; issues a signed, fail-closed decision.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json"
  },
  "dependencies": {
    "@nas/contracts": "workspace:*",
    "@nas/crypto": "workspace:*",
    "@nas/kernel-evidence": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.7.2"
  }
}
